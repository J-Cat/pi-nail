import * as http from "http";
import { IncomingMessage, OutgoingMessage, Server } from "http";

export const requestHandler: (request: IncomingMessage, response: OutgoingMessage) => void = (request, response) => {
};